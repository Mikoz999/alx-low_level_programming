#include "main.h"

/**
 * _isdigit - writes a function that check for  digit (0 through 9)
 * @c:char to chec
 *
 * Return: 0 or 
 */

int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	
		return (1);
	
	else
	
		return (0);
	
}
			

